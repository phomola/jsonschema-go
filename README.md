# jsonschema-go

You should probably use [this package](https://github.com/google/jsonschema-go) to work with JSON schemas in Go.
This package only exists for interoperability with [Apple's foundation models](https://developer.apple.com/documentation/foundationmodels/generationschema?ref=createwithswift.com). 
